# Cloudflare Pages 项目配置
# 用于本地开发测试
# 部署时直接连接 GitHub 仓库到 Cloudflare Pages 即可，无需此文件

name = "data-submit-system"
compatibility_date = "2024-12-01"

# 本地开发时需要设置环境变量
# 在 Cloudflare Pages 控制台设置以下环境变量：
#   WPS_WEBHOOK_URL - WPS 多维表自动化流程的 webhook 地址
